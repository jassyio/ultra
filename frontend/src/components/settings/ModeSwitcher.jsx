const ModeSwitcher = () => {
    return (
      <div>
        <h2>Switch Mode</h2>
        {/* Add mode switch logic here */}
      </div>
    );
  };
  
  export default ModeSwitcher; // ✅ Make sure it's a default export
  